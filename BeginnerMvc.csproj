<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <!-- Build for .NET 9. The Web SDK supplies website features. -->
    <TargetFramework>net9.0</TargetFramework>
    <!-- Warn about possibly missing values. -->
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Resend" Version="0.18.0" />
  </ItemGroup>

</Project>

